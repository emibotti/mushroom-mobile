import { StyleSheet } from 'react-native'

export const styles = StyleSheet.create({
  conditionsContainer: {
    flex: 1,
  },
  passwordConditionContainer: {
    alignItems: 'center',
    flexDirection: 'row',
    marginVertical: 4,
  },
})
