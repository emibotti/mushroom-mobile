import { StyleSheet } from 'react-native'
import { normalizedHeight } from 'src/styles/scale'

export const styles = StyleSheet.create({
  headerIcon: {
    justifyContent: 'flex-start',
    paddingTop: normalizedHeight(1),
  },
})
