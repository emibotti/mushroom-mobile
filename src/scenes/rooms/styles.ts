import { StyleSheet } from 'react-native'

export const styles = StyleSheet.create({
  addRoomButton: {
    marginBottom: 20,
  },
  container: {
    alignItems: 'center',
    flex: 1,
    justifyContent: 'center',
    marginHorizontal: 0,
    marginTop: 210,
    paddingHorizontal: 0,
  },
})
