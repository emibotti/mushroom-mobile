import { StyleSheet } from 'react-native'

export const styles = StyleSheet.create({
  container: {
    alignItems: 'center',
    flex: 1,
    justifyContent: 'center',
  },
  headerLeft: {
    marginLeft: 15,
    marginTop: 20,
  },
  headerRight: {
    marginRight: 15,
    marginTop: 20,
  },
})
